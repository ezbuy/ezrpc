/**
 * This file is autogenerated by ezrpc
 * Don't change manually
 */
 
using System;

namespace {{.Namespace}} {
	public static void onMsg(object sender, MsgHandlerEventArgs e) {
		Console.WriteLine(e.Message.Subject);

		var trans = new Thrift.Transport.TMemoryBuffer(e.Message.Data);
		var iprot = new EZProto(trans);
		try {
			switch (e.Message.Subject) {
			// {{range := }}
			// case "":
			// 	break;
			// {{end}}
			
			// case "Product.Ping":
			// 	processor.Ping_Process(0, iprot, null);
			// 	break;
			// case  "Product.GetProductDetail":
			// 	var trans2 = new Thrift.Transport.TMemoryBuffer();
			// 	var oprot = new EZProto(trans2);
			// 	processor.GetProductDetail_Process(0, iprot, oprot);

			// 	var msg = new Msg();
			// 	msg.Subject = e.Message.Reply;
			// 	msg.Data = trans2.GetBuffer();
				
			// 	Console.WriteLine(msg.Data.Length);
				
			// 	var sub = (IAsyncSubscription)sender;
			// 	sub.Connection.Publish(msg);
			// 	break;
			default:
			}
		} catch (Exception ex) {
			Console.WriteLine(ex.ToString());
		}
	}	
}